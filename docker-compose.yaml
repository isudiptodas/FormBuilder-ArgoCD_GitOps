version: '3.8'
services:
  frontend:
    build: ./frontend
    ports:
      - "5173:5173"
    env_file:
      - ./frontend/.env
  backend: 
    build: ./backend
    ports:
      - "5000:5000"
    env_file:
      - ./backend/.env